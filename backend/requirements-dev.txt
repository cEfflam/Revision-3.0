# Outils de développement — jamais installés dans l'image de production.
#   docker compose exec backend pip install -r requirements-dev.txt
-r requirements.txt

pytest>=8.3,<9
pytest-asyncio>=0.24,<1
ruff>=0.8,<1
